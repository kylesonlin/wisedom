import { readFileSync } from 'fs';
import { join } from 'path';

const content = readFileSync(join(process.cwd(), 'pages/docs/api.mdx'), 'utf-8');

export default function ApiDocsPage() {
  return (
    <div className="prose dark:prose-invert max-w-none">
      {content}
    </div>
  );
} 